{
  "packages": {
    "node": "20"
  },
  "build": {
    "cmd": "composer install --no-dev --optimize-autoloader --no-interaction --prefer-dist && php artisan filament:assets && npm ci && npm run build"
  },
  "start": {
    "cmd": "php artisan optimize:clear && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}
